comments bij media per document opslaan, niet per foto/filmpje
fix: streams get added multiple times

autoupdate metadata filmpjes

search documents/media (zowel zowel frontend als backend)
    -functionaliteit ingebouwd
    -nog niet in de interface geïntegreerd

upgrade cleditor to ckeditor (wysiwyg text editor)

multivalue fieldtypes:
    -selectbox fields
    -checkbox fields
    -radio fields
calculated fields

factuurnr. field
factuur herinnering

folders for media
subfolders

add multiple videos (instead of just single one)
